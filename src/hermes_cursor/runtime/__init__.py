"""Cursor SDK runtime helpers (future harness / streaming CLI).

MVP ships CLI + skill; these modules are adapted from the legacy fork for
SDK 1.x cutover and optional future external-harness hooks.
"""
